########################################################################
#                         Clippy Configuration                         #
#                                                                      #
# Controls the `cargo clippy` linter for the lints where it accepts    #
# external input. Relatively few lints do; most require control        #
# attributes set directly in the source code.                          #
########################################################################

single-char-binding-names-threshold = 8
too-many-arguments-threshold = 8
